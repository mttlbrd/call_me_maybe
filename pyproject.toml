[project]
name = "call-me-maybe"
version = "0.1.0"
description = "Call Me Maybe project"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "llm-sdk",
    "pydantic>=2.12.5",
    "mypy>=1.19.1",
    "flake8>=7.3.0",
]

[tool.uv.workspace]
members = [
    "llm_sdk",
]

[tool.uv.sources]
llm-sdk = { workspace = true }